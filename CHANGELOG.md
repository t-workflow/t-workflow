# Changelog

Each release lists what changed and, when an update needs something the installer
cannot do, a **Consumer actions** entry. `/t-update` reads this file.

## Unreleased

First cut: the contract, nine skills, the gate scripts, one CI workflow, the installer.
